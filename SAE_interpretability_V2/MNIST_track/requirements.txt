# PyTorch (for CUDA 12.1 - see below for CPU-only)
# Install PyTorch first with: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# For CPU-only: pip install torch torchvision torchaudio

# Core dependencies
pytorch-lightning<3
numpy>=1.24,<2  # IMPORTANT: Must be <2 for compatibility with pytorch-lightning and matplotlib
matplotlib>=3.5.0

# Transformer and interpretability tools
transformer_lens
# Tracr (install separately): pip install git+https://github.com/neelnanda-io/Tracr

# Note: Install PyTorch first, then run: pip install -r requirements.txt
# Then install Tracr separately: pip install git+https://github.com/neelnanda-io/Tracr

